{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Vetryx Rule File",
  "description": "Schema for Vetryx security detection rules",
  "type": "object",
  "required": ["category", "rules"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "category": {
      "type": "string",
      "description": "Category name for this group of rules"
    },
    "rules": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/rule"
      }
    }
  },
  "definitions": {
    "rule": {
      "type": "object",
      "required": ["id", "title", "description", "severity", "pattern"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique rule identifier (e.g., EXEC-001)",
          "pattern": "^[A-Z]+-[0-9]+$"
        },
        "title": {
          "type": "string",
          "description": "Short descriptive title"
        },
        "description": {
          "type": "string",
          "description": "Detailed description of what this rule detects and why it's dangerous"
        },
        "severity": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low", "info"],
          "description": "Severity level of findings from this rule"
        },
        "pattern": {
          "type": "string",
          "description": "Regular expression pattern to match"
        },
        "file_extensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "File extensions to apply this rule to (empty = all files)"
        },
        "remediation": {
          "type": "string",
          "description": "Guidance on how to fix issues found by this rule"
        },
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this rule is enabled"
        }
      }
    }
  }
}
